1. Probe formats
`iso-ripper probe /path/to/isos/`
2. Index everything (creates one .txt per ISO)
`iso-ripper index /path/to/isos/ /path/to/output/`
3. Extract all MP3s
`iso-ripper extract /path/to/isos/ /path/to/output/ --ext mp3`

